
Процедура ОбработкаПроведения(Отказ, РежимПроведения)

    // очищаем движения при перепроведении
    Движения.ДвижениеДенег.Очистить();

    // разрешаем запись движений
    Движения.ДвижениеДенег.Записывать = Истина;

    // добавляем движение в регистр
    Движение = Движения.ДвижениеДенег.Добавить();
    Движение.Период    = Дата;
    Движение.Касса     = Касса;
    Движение.СтатьяДДС = СтатьяДДС;
    Движение.Сумма = СуммаДокумента;

КонецПроцедуры
